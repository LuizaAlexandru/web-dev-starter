//lesson for objects and symbols

let person = {
    firstName: 'Luiza',
    lastName: 'Alexandru'
}
showMessage(person.firstName + person.lastName);
//showMessage(typeof person);
//lesson for null and undefined

//let saved = 10;
//saved = null;
//showMessage(saved);
//console.log(saved);
//let saved;
//showMessage(saved);
//console.log(saved); // undefined


//lesson for boolean number

//let saved = true;
//saved = !saved;
//showMessage(saved);


// lesson for Converting strings and number

//let amount = 123;
//amount = amount.toString();
//showMessage(amount);
//showMessage(typeof amount);
//let amount = Number.parseFloat("123.12A");
//
//showMessage(amount);

//lesson for manipulating strings


//message = message + ' World!';
//showMessage(message);
//message = message.length; //substring(hides the number of letters in the string that is specified);
//shouMessage(message);
//let message = '1234';
//showMessage(message + 2); //12342

//lesson for strings

//let message = "Hello \"World!\"";
//showMessage(message);
//let name = 'Luiza';
//let message = `Hello ${name}`; // utilise quotation marks next to 1
//showMessage(message);

// lesson for negative number

//let amount = 20 - -2;
//
//showMessage(amount);

//let amount = 0;
//
//showMessage(--amount);

//lesson for number

//let price = 20.99;
//   taxRate= 0.07;
//price = price + 1; // other operation (-, /, %, *; +=; -=; *=; %=; /=)

//showMessage(price);
//showMessage(--price);
//console.log(price);
//showMessage(++price);
//showMessage(price + taxRate);
